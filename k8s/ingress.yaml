apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: redirect-https
spec:
  redirectScheme:
    scheme: https
    permanent: true
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: express
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    traefik.ingress.kubernetes.io/router.middlewares: express-redirect-https@kubernetescrd
spec:
  ingressClassName: traefik
  tls:
    - hosts: [express.example.sslip.io]   # lo sustituye la Application de ArgoCD
      secretName: express-tls
  rules:
    - host: express.example.sslip.io      # lo sustituye la Application de ArgoCD
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: express
                port:
                  number: 80
